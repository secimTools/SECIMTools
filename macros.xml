<macros>
<token name="@TIP_AND_WARNING@">
.. class:: infomark

**TIP:** If your data is not TAB delimited, use *Text Manipulation-&gt;Convert*

.. class:: warningmark

**WARNINGS:**

    - (1) A row will be dropped when a pairwise comparison has a missing value.
    - (2) This script automatically removes spaces and special characters from strings.
    - (3) If a compound/feature name starts with a number it will prepend an '_'.
    - (4) Input names are case sensitive and must match exactly (e.g use 'Feature' not 'feature').

--------------------------------------------------------------------------------------------------

</token>
<token name="@WIDE@">
    **Wide Formatted Dataset**

    A wide formatted dataset that contains measurements for each sample.::
    
        +----------+---------+---------+---------+-----+
        | Feature | sample1 | sample2 | sample3 | ... |
        +==========+=========+=========+=========+=====+
        | one      | 10      | 20      | 10      | ... |
        | two      | 5       | 22      | 30      | ... |
        | three    | 30      | 27      | 2       | ... |
        | four     | 32      | 17      | 8       | ... |
        | ...      | ...     | ...     | ...     | ... |
        +----------+---------+---------+---------+-----+

    **NOTE: The 'Feature' column defines the rows within a wide formatted dataset.
</token>
<token name="@METADATA@">
    **Metadata File:** A Metadata file relating samples to various
    groups/treatment.::

        +----------+--------+
        | sampleID | group |
        +==========+========+
        | sample1  | g1     |
        | sample2  | g1     |
        | sample3  | g1     |
        | sample4  | g2     |
        | sample5  | g2     |
        | sample6  | g2     |
        | ...      | ...    |
        +----------+--------+

    **NOTE: You must have a column named *sampleID*** and the values in this
    column must match the column names in the wide dataset.
</token>
<token name="@GROUP_OPTIONAL@">
    **Group/Treatment** [Optional]

    - The column name in your metadata file that contains group information. In
      our example metadata file we would put *group*. If provided samples will
      be colored by group.

</token>
<token name="@GROUP@">
    **Group/Treatment**

    - The column name in your metadata file that contains group information. In
      our example metadata file we would put *group*.

</token>
</macros>
