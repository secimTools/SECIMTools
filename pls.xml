<tool id="pls" name="Partial Least Squares (PLS) (Beta)" version="1.0.0">
    <description>"Adjust p-values based on different methods."</description>
    <requirements>
        <requirement type="python-module">sklearn</requirement>
        <requirement type="python-module">pandas</requirement>
        <requirement type="python-module">numpy</requirement>
        <requirement type="python-module">matplotlib</requirement>
        <requirement type="python-module">palettable</requirement>
    </requirements>
    <command interpreter="python">
        pls.py
        --input $input
        --design $design
        --ID $uniqID
        --group $group
        --toCompare $toCompare
        --nComp $nComp
        --outScores $outScores
        --outWeights $outWeights
        --figure $figures
    </command>
    <inputs>
        <param name="input" type="data" format="tabular" label="Wide Dataset" help="Input dataset in wide format and tab separated. If file is not tab separated see TIP below."/>
        <param name="design" type="data" format="tabular" label="Design File" help="Design file tab separated. Note you need a 'sampleID' column. If not tab separated see TIP below"/>
        <param name="uniqID" type="text" size="30" value="" label="Unique Feature ID" help="Name of the column in your Wide Dataset that has unique Feature IDs."/>
        <param name="group" type="text" size="30" label="Group/Treatment [Optional]" help="Enter group name to calculate from. Spelling and capitalization must be exact."/>
        <param name="toCompare" type="text" size="30" label="Name of the groups to compare" help="Comma separated names of the 2 groups in your Group/Treatment column that you want to compare."/>
        <param name="nComp" type="text" size="30" value="2" label="Number of componentes [Optional]" help="Enter number of components to use to perform the analysis default:2."/>
    </inputs>
    <outputs>
        <data format="tabular" name="outScores" label="${tool.name} on ${on_string}: scores"/>
        <data format="tabular" name="outWeights" label="${tool.name} on ${on_string}: weights"/>
        <data format="pdf" name="figures" label="${tool.name} on ${on_string}: scatter plots"/>
    </outputs>
    <macros>
        <import>macros.xml</import>
    </macros>
<help>

@TIP_AND_WARNING@

.. class:: infomark

**What it does**

This tool performs Partial Least Sqares Analysis on the numeric input data.

--------------------------------------------------------------------------------

.. class:: warningmark

**Note**

- This tool currently treats all variables as continuous numeric
  variables. Running the tool on categorical variables might result in
  incorrect results. 
- Rows containing non-numeric (or missing) data in any
  of the chosen columns will be skipped from the analysis.

--------------------------------------------------------------------------------

**Input**

- Two input datasets are required.

    @WIDE@

    **NOTE:** The sample IDs must match the sample IDs in the Design File (below).
    Extra columns will automatically be ignored.


    @METADATA@

**In addition to your datasets, you need to provide:**

**Unique Feature ID**

- The column name in your wide dataset that contains the unique IDs for
  your features. In our example dataset you would input *Feature*.

--------------------------------------------------------------------------------

**Output Files**

- A PDF containing the 2D plots for all possible level combinations.

- A TSV containing scores of the model.

- A TSV containing weights of the model.

</help>
</tool>
