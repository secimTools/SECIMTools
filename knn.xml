<tool id="knn2" name="K Nearest Neighbors Imputation (KNN)" version="0.0.5">
    <description>"Attempts to fill missing data by K Nearest Neighbors algorithm"</description>
    <requirements>
        <requirement type="python-module">rpy2</requirement>
        <requirement type="python-module">pandas</requirement>
        <requirement type="python-module">numpy</requirement>
    </requirements>
    <command interpreter="python">
        knn.py
        --input $input
        --design $design
        --ID $uniqID
        --group $group
        --output $imputed
        --knn $k
        --row_cutoff $rowCutoff
        --col_cutoff $colCutoff
        #if $noZero
            --no_zero
        #end if

        #if $noNeg
            --no_negative
        #end if

        #if $exclude
            --exclude $exclude
        #end if
    </command>
    <inputs>
        <param name="input" type="data" format="tabular" label="Wide Dataset" help="Input dataset in wide format and tab separated. If file is not tab separated see TIP below."/>
        <param name="design" type="data" format="tabular" label="Design File" help="Design file tab separated. Note you need a 'sampleID' column. If not tab separated see TIP below"/>
        <param name="uniqID" type="text" size="30" value="" label="Unique Feature ID" help="Name of the column in your Wide Dataset that has unique Feature IDs."/>
        <param name="group" type="text" size="30" label="Input Group name to separate by [OPTIONAL]" help="Enter group name to calculate from. Spelling and capitalization must be exact"/>

        <param name="noZero" type="boolean" label="Count Zeroes as missing" help="Zeroes can be counted as missing or left as data"/>
        <param name="noNeg" type="boolean" label="Count Negative as missing" help="Negatives can be counted as missing or left as data"/>
        <param name="exclude" type="text" size="30" value="" label="Additional values to treat missing" help="Seperate additional values to count as missing data with commas"/>

        <param name="k" type="text" size="30" value="5" label="K value" help="K is the number of neighbors to search. Default: 5"/>
        <param name="rowCutoff" type="text" size="30" value=".5" label="Row Percent Cutoff Value" help="If this is exceeded, imputation will be done by mean instead of knn. Default: .5 (50%)"/>
        <param name="colCutoff" type="text" size="30" value=".8" label="Column Percent Cutoff Value" help="If this is exceeded, imputation will be done by mean instead of knn. Default: .8 (80%)"/>
    </inputs>
    <outputs>
        <data format="tabular" name="imputed" label="${tool.name} on ${on_string}: imputed"/>
    </outputs>
    <macros>
        <import>macros.xml</import>
    </macros>
<help>

@TIP_AND_WARNING@

.. class:: infomark

**What it does**

This tool imputes missing data in a dataset.

--------------------------------------------------------------------------------

.. class:: warningmark

**Note**

- This tool currently treats all variables as continuous numeric
  variables. Running the tool on categorical variables might result in
  incorrect results. 
- Rows containing non-numeric (or missing) data in any
  of the chosen columns will be skipped from the analysis.

--------------------------------------------------------------------------------

**Input**

- Two input datasets are required.

    @WIDE@

    **NOTE:** The sample IDs must match the sample IDs in the Design File (below).
    Extra columns will automatically be ignored.


    @METADATA@

**In addition to your datasets, you need to provide:**

**Unique Feature ID**

- The column name in your wide dataset that contains the unique IDs for
  your features. In our example dataset you would input *Feature*.

--------------------------------------------------------------------------------

**Output Files**

- This tool outputs the wide dataset with missing values filled in



**  

</help>
</tool>
