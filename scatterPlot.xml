<tool id="scatterPlot" name="Scatter Plot">
    <description>Create a 2d scatter plot plots.</description>
    <requirements>
        <requirement type="python-module">pandas</requirement>
        <requirement type="python-module">matplotlib</requirement>
    </requirements>
    <command interpreter="python">scatterPlot.py --input $input --ID $uniqID
        #if $design and $group
        --design $design
        --group $group
        #end if

        --xaxis $xaxis
        --yaxis $yaxis

        #if "$title"
            --title "$title"
        #end if

        #if $xname
            --xname $xname
        #end if
        #if $yname
            --yname $yname
        #end if

        --fig $fig
    </command>
    <inputs>
        <param name="input" type="data" format="tabular" label="Stacked Dataset"
               help="Input dataset in wide format and tab separated. If not tab separated see TIP below."/>
        <param name="uniqID" type="text" size="30" value="sampleID" label="Sample ID"
               help="Name of the column in your Wide Dataset that has unique compound/gene IDs."/>
        <param name="xaxis" type="text" size="30" value="" label="Column name with X-value"/>
        <param name="yaxis" type="text" size="30" value="" label="Column name with Y-value"/>
        <param name="title" type="text" size="30" value="" optional="true" label="Plot title [Optiona]."/>
        <param name="design" type="data" format="tabular" optional="true" label="Metadata File [Optional]."
               help="Metadata file tab separated. Note you need a 'sampleID' column. If not tab separated see TIP below"/>
        <param name="group" type="text" size="30" value="" optional="true" label="Group/Treatment [Optional]"
               help="Name of the column in your Metadata File that contains group classifications."/>
        <param name="xname" type="text" size="30" value="" optional="true" label="X-axis label [Optional]."/>
        <param name="yname" type="text" size="30" value="" optional="true" label="Y-axis label [Optional]."/>
    </inputs>
    <outputs>
        <data format="png" name="fig"/>
    </outputs>
<help>

..class:: infomark

**TIP:**
If your data is not TAB delimited, use *Text Manipulation-&gt;Convert*

.. class:: warningmark

**WARNINGS:**
    - (1) This script automatically removes spaces and special characters from strings.
    - (2) If a compound name starts with a number it will prepend an '_'.

--------------------------------------------------------------------------------

**What it does**

Generates a 2 dimensional scatter plot.

--------------------------------------------------------------------------------

**Input**

- Two input datasets are required.

**Tall Formatted Dataset:**

A tall formatted dataset that contains measurements for each sample.::

    +----------+-----------+-----------+-----------+-----+
    | sampleID | feature1  | feature1  | feature2  | ... |
    +==========+===========+===========+===========+=====+
    | sample1  |    10     |    20     |    10     | ... |
    | sample2  |    5      |    22     |    30     | ... |
    | sample3  |    30     |    27     |    2      | ... |
    | sample4  |    32     |    17     |    8      | ... |
    | ...      |    …      |    ...    |    …      | ... |
    +----------+-----------+-----------+-----------+-----+

    **NOTE:** The sample IDs must match the sample IDs in the Metadata File (below).


**Metadata File:**
A metatdata file relating samples to various groups/treatment.::

    +----------+--------+
    | sampleID | group1 |
    +==========+========+
    | sample1  | g1     |
    | sample2  | g1     |
    | sample3  | g1     |
    | sample4  | g2     |
    | sample5  | g2     |
    | sample6  | g2     |
    | ...      | ...    |
    +----------+--------+

    **NOTE: You must have a column named *sampleID***
    and the values in this column must match the vlues in column in the tall
    formatted dataset.

- In addition to your datasets, you need to provide:

**Sample ID**

    - The column name in your Tall and Metadata datasets that contains the unique IDs for your samples.  In our example dataset you would input *SampleID*.

**Column name with X-value**

    - The column name in your Tall dataset that contains the values to be plotted on the X-axis.

**Column name with Y-value**

    - The column name in your Tall file that contains the values to be plotted on the Y-axis.

**Plot title**  [Optional]

    - Optional title to display on your plot


**Group/Treatment**  [Optional]

    - The column name in your Metadata file that contains group information. In
      our example design file we would put *group1*.


**X-axis label**  [Optional]

    - Optional x-axis label to display on your plot


**Y-axis label**  [Optional]

    - Optional y-axis label to display on your plot

--------------------------------------------------------------------------------

**Output Files**

- This tool outputs 1 PDF file:

    **scatterplot_PDF** is a PDF plot of the selected values.


</help>
</tool>

